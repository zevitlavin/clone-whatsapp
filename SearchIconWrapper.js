import { styled } from "@mui/material";

const SearchIconWrapper = styled("div")(({ theme }) => ({
    padding: theme.spacing(1.5, 31),
    height: "100%",
    position: "absolute",
    pointerEvents: "none",
    display: "flex",
    alignContent: "center",
    justifyContent: "center",
  
  
  }));
  export default SearchIconWrapper;